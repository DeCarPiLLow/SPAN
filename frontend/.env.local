# Local development - Vite proxies /api to localhost:5000 automatically (see vite.config.js)
# No need to set VITE_API_BASE_URL - leave blank to use the proxy
VITE_API_BASE_URL=http://127.0.0.1:5000/api
